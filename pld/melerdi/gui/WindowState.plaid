package melerdi.gui;

state WindowState
{
	method setupPeer()
	{
     	this.peer.addWindowListener(
		plaidgui.auxiliary.WindowListener.new(fn() => notifyClose()));
		this <- UndisplyableWindow;
		this <- UnsizedWindow;
		this <- UnpositionedWindow;
		this <- EnabledWindow;
	}
}